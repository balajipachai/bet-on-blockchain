pragma solidity ^0.5.0;

import "../interface/IBetCondition.sol";

/// @title DoubleStakesAboutBet
/// @author Balaji Shetty Pachai
/// @dev A Double Stakes About is an Any To Come bet consisting of two selections partaking in different events, which are compiled to produce two bets derived from two singles. Any return on each single, up to double the original stake amount, is used to fund a another single on the other selection.
contract DoubleStakesAboutBet is IBetCondition {

}
