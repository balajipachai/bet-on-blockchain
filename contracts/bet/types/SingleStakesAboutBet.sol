pragma solidity ^0.5.0;

import "../interface/IBetCondition.sol";

/// @title SingleStakesAboutBet
/// @author Balaji Shetty Pachai
/// @dev A Single Stakes About is an Any To Come bet consisting of two selections from events, these are then compiled to produce two bets derived from two singles. Any return on each single (up to the original stake amount) is used to fund a further single on the other selection.
contract SingleStakesAboutBet is IBetCondition {

}
